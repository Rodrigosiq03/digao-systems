server:
  port: 8081
  forward-headers-strategy: framework

spring:
  application:
    name: digao-oauth-service

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/digao-oauth-dev
  rabbitmq:
    port: ${RABBIT_PORT:5672}
    username: ${RABBIT_USER:dev}
    password: ${RABBIT_PASS:password}
    host: ${RABBIT_HOST:localhost}
    virtual-host: ${SPRING_RABBITMQ_VIRTUAL_HOST:/dev}

  web:
    error:
      include-message: always
      include-binding-errors: always

keycloak:
  admin:
    server-url: http://localhost:8080
    realm: digao-oauth-dev
    client-id: digao-oauth-backend-admin
    client-secret: ${KEYCLOAK_ADMIN_CLIENT_SECRET}

digao:
  rabbitmq:
    exchange: ${RABBIT_EXCHANGE:notification.exchange}
    routing-key: ${RABBIT_ROUTING_KEY:digao.auth.email.send}
  notification:
    login-url: ${DIGAO_PORTAL_URL:http://localhost:3000}
  cors:
    allowed-origins: ${DIGAO_PORTAL_ORIGINS:http://localhost:3000}
  vpn-sync:
    enabled: ${VPN_SYNC_ENABLED:false}
    cron: ${VPN_SYNC_CRON:0 */15 * * * *}
    provider: ${VPN_SYNC_PROVIDER:tailscale}
    api-base-url: ${VPN_SYNC_API_BASE_URL:https://api.tailscale.com}
    tailnet: ${VPN_SYNC_TAILNET:}
    api-token: ${VPN_SYNC_API_TOKEN:}
    user-page-size: ${VPN_SYNC_USER_PAGE_SIZE:200}

logging:
  level:
    com.digao.digao_oauth_service.infra.identity.keycloak: DEBUG
    org.keycloak.admin: DEBUG
    org.jboss.resteasy.client: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  prometheus:
    metrics:
      export:
        enabled: true
  tracing:
    enabled: true
    sampling:
      probability: ${TRACING_SAMPLING_PROBABILITY:1.0}
  otlp:
    tracing:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://otel-collector:4318/v1/traces}
  observations:
    key-values:
      application: ${spring.application.name}

  
